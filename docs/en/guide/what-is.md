# What is starter-lib-ts?

`starter-lib-ts` is a TypeScript library starter template that provides VitePress online documentation and supports building both ESM (ECMAScript Module) and CJS (CommonJS) formats.
